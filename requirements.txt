fastapi>=0.115
uvicorn>=0.30
requests>=2.32
pandas>=2.2.3
beautifulsoup4>=4.12
# curl_cffi and playwright are only needed for the NSE-based classification
# attempts (classify_industries.py / classify_industries_playwright.py).
# The recommended path, classify_via_screener.py, only needs requests +
# beautifulsoup4 above.
curl_cffi>=0.7
playwright>=1.45
